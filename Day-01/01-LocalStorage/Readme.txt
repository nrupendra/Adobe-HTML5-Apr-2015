LocalStorage
    - Key/Value Store
    - Both key and value should be strings
    - 10MB / per origin
    - Same origin policy applied
    
    - window.localStorage
    - APIs
        * setItem(key,value)
        * getItem(key) //=> value
        * removeItem(key)
        * key(index) //=> key
        * clear()
        * length